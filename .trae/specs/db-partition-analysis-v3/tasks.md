# Tasks

- [x] Task 1: 提取设计文档表结构
  - [x] 读取 `EE-湖北农信新信贷数据库设计文档\` 下全部 23 个 `.xlsx` 文件
  - [x] 跳过"修订记录"、"目录"、"索引目录"等非表结构 Sheet
  - [x] 对每个表结构 Sheet，提取：表英文名、表中文名、所有字段（字段名、类型、中文注释、是否主键）
  - [x] 关联所属模块（从文件名推导，如"合同管理v1.0.1" → "合同管理"）
  - [x] 输出：`table_structures.json`（1848张表的完整字段结构映射）

- [x] Task 2: 构建生产数据量基准
  - [x] 读取 `AA-数据量\老信贷生产环境迁移表数据量.xlsx`
  - [x] 建立 `{表英文名 → 当前数据量}` 映射（188张表）
  - [x] 去重处理（同一表名多条记录取最大数据量）
  - [x] 输出：`prod_data_volume.json`

- [x] Task 3: AI 逐表业务语义分析（分批处理）
  - [x] 1848张表按信贷业务语义分析（基于字段结构+业务知识+生产数据量）
  - [x] 对每张表输出：业务角色、增长模式、3-5年预估数据量、是否需要分区
  - [x] 汇总所有分析结果 → `ai_analysis_result.json`
  - **注意**：核心步骤，基于字段结构+信贷业务知识推理，非简单规则匹配

- [x] Task 4: AI 分区键选择与分区数计算（仅需分区表）
  - [x] 对21张需分区表，从字段列表中选分区键（排除 TENANT_ID）
  - [x] 计算分区数 = CEILING(预估量 / 300万)，≤ 50
  - [x] 生成分区策略描述
  - **注意**：日志类表分区键 = CREATE_TIME，分区数 = 0

- [x] Task 5: 生成最终分区清单 Excel
  - [x] 合并 AI 分析结果 + 人工标注基线（责任人信息）
  - [x] 生成完整的 11 列 Excel
  - [x] 所有单元格填写完整，无空值
  - [x] 输出：`湖北农信数据库分区表清单_精细化版_v3.xlsx`

- [x] Task 6: 质量校验
  - [x] 校验：G列="是" 的表，预估数据量 > 1000万（21张全部通过）
  - [x] 校验：G列="否" 的表，预估数据量 ≤ 1000万（1827张全部通过）
  - [x] 校验：分区键无 TENANT_ID（全部通过）
  - [x] 校验：分区数 = CEILING(预估量/300万)，≤ 50（全部通过）
  - [x] 校验：单分区 ≤ 300万（全部通过）
  - [x] 抽查 10 张表验证 AI 分析逻辑的合理性（9/10通过，1个警告为类型细分差异）

# Task Dependencies
- Task 1 和 Task 2 可并行执行（无依赖关系）
- Task 3 依赖 Task 1 和 Task 2（需要表结构 + 数据量基准）
- Task 4 依赖 Task 3（仅在 Task 3 分析完成后，对需分区表执行）
- Task 5 依赖 Task 3 和 Task 4（需要完整的分析结果 + 分区决策）
- Task 6 依赖 Task 5（校验最终输出）