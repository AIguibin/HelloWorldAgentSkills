# Tasks

- [ ] Task 1: 构建生产数据量基准与表结构映射
  - [ ] 读取`老信贷生产环境迁移表数据量.xlsx`，去重后建立`表名→当前数据量`映射
  - [ ] 加载已有的`partition_analysis_data.json`（含1860张表的字段结构）
  - [ ] 建立老系统表名与新系统表名的模糊匹配映射（老系统表名可能大小写不同或略有差异）
  - [ ] 输出：`数据量基准映射表`

- [ ] Task 2: 逐表信贷业务语义分析（分批处理，每批200张表）
  - [ ] 对每张表，读取其完整字段结构（字段名+字段类型+字段中文名）
  - [ ] 结合信贷知识（参考`CC-新信贷基础知识`），分析该表的业务角色：
    - 这张表在信贷系统中做什么？（如：存储对私合同的核心信息）
    - 数据是如何产生的？（如：每次签合同产生一条记录）
    - 数据增长模式是什么？（如：随客户量+业务量增长）
  - [ ] 预估3-5年数据量：
    - 老系统有数据的：`当前量 × 增长系数`
    - 老系统无数据的：基于业务角色和相似表推测，标注"预估"
  - [ ] 判断是否 > 1000万

- [ ] Task 3: 分区键选择与分区数计算
  - [ ] 对于需要分区的表（预估 > 1000万），从字段列表中筛选候选分区键
  - [ ] 排除TENANT_ID（避免法人行数据倾斜）
  - [ ] 按优先级选择：业务主键 > 流水号 > 编号字段 > 时间字段
  - [ ] 验证分区键基数是否足够（字段类型和业务含义上能提供均匀分布）
  - [ ] 计算分区数 = CEILING(预估数据量 / 3,000,000)，最大不超过50

- [ ] Task 4: 生成最终分区清单Excel
  - [ ] 合并所有分析结果（分区判断 + 分区键 + 分区数 + 策略 + 备注）
  - [ ] 保留原有人工标注的93张表的责任人信息
  - [ ] 新增表的责任人填"待确认"
  - [ ] 生成完整的`湖北农信数据库分区表清单_精细化版.xlsx`
  - [ ] 所有11列必须填写完整，无空白

- [ ] Task 5: 质量校验
  - [ ] 校验分区表数量是否合理（预期在100-200张之间，即5%-10%）
  - [ ] 校验无TENANT_ID作为分区键（除非特殊说明）
  - [ ] 校验所有需分区表的分区数 ≥ 1 且 ≤ 50
  - [ ] 校验单分区数据量 ≤ 300万
  - [ ] 抽查10张典型表验证分析逻辑

# Task Dependencies
- Task 2 依赖 Task 1（需要数据量基准）
- Task 3 依赖 Task 2（需要业务分析和数据量预估）
- Task 4 依赖 Task 3（需要分区键和分区数）
- Task 5 依赖 Task 4（校验最终输出）