# aiguibin-platform-arch 架构管理平台 - 数据库设计

## 1. 数据库概述

### 1.1 数据库选型
- **数据库**: MySQL 8.0
- **连接池**: Druid 1.2.22
- **ORM框架**: MyBatis-Plus 3.5.6

### 1.2 数据库列表
| 数据库 | 用途 |
|-------|------|
| platform_config | 平台配置库（主库） |
| platform_archive | 归档数据 |

## 2. 表结构设计

### 2.1 用户管理

#### 2.1.1 用户表 (sys_user)
```sql
CREATE TABLE sys_user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    user_no VARCHAR(50) NOT NULL COMMENT '用户编号（如EMP001）',
    username VARCHAR(50) NOT NULL COMMENT '用户名（登录账号）',
    password VARCHAR(100) NOT NULL COMMENT '密码（BCrypt加密）',
    real_name VARCHAR(50) COMMENT '真实姓名',
    email VARCHAR(100) COMMENT '邮箱',
    phone VARCHAR(20) COMMENT '手机号',
    avatar VARCHAR(200) COMMENT '头像URL',
    org_code VARCHAR(50) COMMENT '默认机构编码',
    dept_code VARCHAR(50) COMMENT '默认部门编码',
    is_locked TINYINT DEFAULT 0 COMMENT '锁定状态 (0-正常 1-锁定)',
    lock_reason VARCHAR(200) COMMENT '锁定原因',
    last_login_time DATETIME COMMENT '最后登录时间',
    last_login_ip VARCHAR(50) COMMENT '最后登录IP',
    status TINYINT DEFAULT 1 COMMENT '状态 (0-禁用 1-启用)',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    create_by BIGINT COMMENT '创建人',
    update_by BIGINT COMMENT '更新人',
    deleted TINYINT DEFAULT 0 COMMENT '删除标记 (0-正常 1-删除)',
    UNIQUE KEY uk_user_no (user_no),
    UNIQUE KEY uk_username (username),
    INDEX idx_org_code (org_code),
    INDEX idx_dept_code (dept_code),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

#### 2.1.2 用户机构关联表 (sys_user_org)
```sql
CREATE TABLE sys_user_org (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    org_code VARCHAR(50) NOT NULL COMMENT '机构编码',
    is_default TINYINT DEFAULT 0 COMMENT '是否默认机构 (0-否 1-是)',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    UNIQUE KEY uk_user_org (user_id, org_code),
    INDEX idx_user_id (user_id),
    INDEX idx_org_code (org_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户机构关联表';
```

### 2.2 机构/部门管理

#### 2.2.1 机构表 (sys_org)
```sql
CREATE TABLE sys_org (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    org_code VARCHAR(50) NOT NULL COMMENT '机构编码',
    org_name VARCHAR(100) NOT NULL COMMENT '机构名称',
    org_type VARCHAR(20) COMMENT '机构类型 (HEAD-总部 BRANCH-分支)',
    parent_code VARCHAR(50) DEFAULT '0' COMMENT '父机构编码',
    parent_codes VARCHAR(500) COMMENT '所有父机构编码（逗号分隔）',
    level INT DEFAULT 1 COMMENT '层级',
    sort_order INT DEFAULT 0 COMMENT '排序',
    status TINYINT DEFAULT 1 COMMENT '状态 (0-禁用 1-启用)',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    create_by BIGINT COMMENT '创建人',
    update_by BIGINT COMMENT '更新人',
    deleted TINYINT DEFAULT 0 COMMENT '删除标记',
    UNIQUE KEY uk_org_code (org_code),
    INDEX idx_parent_code (parent_code),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='机构表';
```

#### 2.2.2 部门表 (sys_dept)
```sql
CREATE TABLE sys_dept (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    dept_code VARCHAR(50) NOT NULL COMMENT '部门编码',
    dept_name VARCHAR(100) NOT NULL COMMENT '部门名称',
    org_code VARCHAR(50) NOT NULL COMMENT '所属机构编码',
    parent_code VARCHAR(50) DEFAULT '0' COMMENT '父部门编码',
    parent_codes VARCHAR(500) COMMENT '所有父部门编码（逗号分隔）',
    manager_id BIGINT COMMENT '部门负责人ID',
    sort_order INT DEFAULT 0 COMMENT '排序',
    status TINYINT DEFAULT 1 COMMENT '状态 (0-禁用 1-启用)',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    create_by BIGINT COMMENT '创建人',
    update_by BIGINT COMMENT '更新人',
    deleted TINYINT DEFAULT 0 COMMENT '删除标记',
    UNIQUE KEY uk_dept_code (dept_code),
    INDEX idx_org_code (org_code),
    INDEX idx_parent_code (parent_code),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='部门表';
```

### 2.3 角色权限管理

#### 2.3.1 角色表 (sys_role)
```sql
CREATE TABLE sys_role (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    role_code VARCHAR(50) NOT NULL COMMENT '角色编码',
    role_name VARCHAR(100) NOT NULL COMMENT '角色名称',
    role_type VARCHAR(20) DEFAULT 'custom' COMMENT '角色类型 (system-系统 custom-自定义)',
    data_scope TINYINT DEFAULT 1 COMMENT '数据范围 (1-全部 2-本机构 3-本部门 4-本人)',
    remark VARCHAR(200) COMMENT '备注',
    status TINYINT DEFAULT 1 COMMENT '状态 (0-禁用 1-启用)',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    create_by BIGINT COMMENT '创建人',
    update_by BIGINT COMMENT '更新人',
    deleted TINYINT DEFAULT 0 COMMENT '删除标记',
    UNIQUE KEY uk_role_code (role_code),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';
```

#### 2.3.2 用户角色关联表 (sys_user_role)
```sql
CREATE TABLE sys_user_role (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    role_id BIGINT NOT NULL COMMENT '角色ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_role (user_id, role_id),
    INDEX idx_role_id (role_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户角色关联表';
```

#### 2.3.3 菜单表 (sys_menu)
```sql
CREATE TABLE sys_menu (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    menu_code VARCHAR(50) NOT NULL COMMENT '菜单编码',
    menu_name VARCHAR(100) NOT NULL COMMENT '菜单名称',
    parent_id BIGINT DEFAULT 0 COMMENT '父菜单ID',
    menu_type TINYINT COMMENT '菜单类型 (1-目录 2-菜单 3-按钮)',
    icon VARCHAR(50) COMMENT '图标',
    path VARCHAR(200) COMMENT '路由路径',
    component VARCHAR(200) COMMENT '组件路径',
    permission VARCHAR(100) COMMENT '权限标识（如:user:add）',
    sort_order INT DEFAULT 0 COMMENT '排序',
    status TINYINT DEFAULT 1 COMMENT '状态 (0-禁用 1-启用)',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    create_by BIGINT COMMENT '创建人',
    update_by BIGINT COMMENT '更新人',
    deleted TINYINT DEFAULT 0 COMMENT '删除标记',
    UNIQUE KEY uk_menu_code (menu_code),
    INDEX idx_parent_id (parent_id),
    INDEX idx_menu_type (menu_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜单表';
```

#### 2.3.4 角色菜单关联表 (sys_role_menu)
```sql
CREATE TABLE sys_role_menu (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    role_id BIGINT NOT NULL COMMENT '角色ID',
    menu_id BIGINT NOT NULL COMMENT '菜单ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_role_menu (role_id, menu_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色菜单关联表';
```

### 2.4 审计日志

#### 2.4.1 操作日志表 (sys_oper_log)
```sql
CREATE TABLE sys_oper_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    oper_type VARCHAR(50) COMMENT '操作类型 (ADD/UPDATE/DELETE/QUERY/LOGIN/LOGOUT)',
    title VARCHAR(100) COMMENT '操作模块',
    method VARCHAR(200) COMMENT '请求方法',
    request_url VARCHAR(500) COMMENT '请求URL',
    request_method VARCHAR(10) COMMENT '请求方式 (GET/POST/PUT/DELETE)',
    request_params TEXT COMMENT '请求参数',
    response_data TEXT COMMENT '响应数据',
    user_id BIGINT COMMENT '操作用户ID',
    username VARCHAR(50) COMMENT '操作用户名',
    org_code VARCHAR(50) COMMENT '操作机构',
    oper_ip VARCHAR(50) COMMENT '操作IP',
    oper_location VARCHAR(100) COMMENT '操作地点',
    status TINYINT COMMENT '操作状态 (0-失败 1-成功)',
    error_msg TEXT COMMENT '错误信息',
    oper_time DATETIME COMMENT '操作时间',
    cost_time BIGINT COMMENT '耗时(毫秒)',
    INDEX idx_user_id (user_id),
    INDEX idx_oper_time (oper_time),
    INDEX idx_oper_type (oper_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';
```

#### 2.4.2 登录日志表 (sys_login_log)
```sql
CREATE TABLE sys_login_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    user_id BIGINT COMMENT '用户ID',
    username VARCHAR(50) COMMENT '用户名',
    login_type VARCHAR(20) COMMENT '登录类型 (PASSWORD/SMS/TOKEN)',
    login_ip VARCHAR(50) COMMENT '登录IP',
    login_location VARCHAR(100) COMMENT '登录地点',
    user_agent VARCHAR(500) COMMENT '浏览器UA',
    login_status TINYINT COMMENT '登录状态 (0-失败 1-成功)',
    fail_reason VARCHAR(200) COMMENT '失败原因',
    login_time DATETIME COMMENT '登录时间',
    INDEX idx_user_id (user_id),
    INDEX idx_login_time (login_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='登录日志表';
```

### 2.5 数据库比对模块

#### 2.5.1 数据源配置表 (db_datasource_config)
```sql
CREATE TABLE db_datasource_config (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    env VARCHAR(10) NOT NULL COMMENT '环境 (DEV/SIT/UAT)',
    ds_key VARCHAR(50) NOT NULL COMMENT '数据源标识',
    ds_name VARCHAR(100) NOT NULL COMMENT '数据源名称',
    db_name VARCHAR(100) NOT NULL COMMENT '数据库名',
    db_type VARCHAR(20) DEFAULT 'mysql' COMMENT '数据库类型',
    host VARCHAR(100) NOT NULL COMMENT '主机',
    port INT DEFAULT 3306 COMMENT '端口',
    username VARCHAR(100) NOT NULL COMMENT '用户名',
    password VARCHAR(200) NOT NULL COMMENT '密码(加密)',
    connection_params VARCHAR(500) COMMENT '连接参数',
    enabled TINYINT DEFAULT 1 COMMENT '是否启用',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_ds_key (ds_key),
    INDEX idx_env (env)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='数据源配置表';
```

#### 2.5.2 SVN配置表 (db_svn_config)
```sql
CREATE TABLE db_svn_config (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    config_name VARCHAR(50) DEFAULT 'default' COMMENT '配置名称',
    svn_url VARCHAR(500) NOT NULL COMMENT 'SVN仓库地址',
    username VARCHAR(100) NOT NULL COMMENT '用户名',
    password VARCHAR(200) NOT NULL COMMENT '密码(加密)',
    work_copy_path VARCHAR(500) NOT NULL COMMENT '工作副本路径',
    doc_relative_path VARCHAR(200) DEFAULT 'docs' COMMENT '文档相对路径',
    result_relative_path VARCHAR(200) DEFAULT 'results' COMMENT '结果相对路径',
    enabled TINYINT DEFAULT 1 COMMENT '是否启用',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='SVN配置表';
```

#### 2.5.3 飞秋配置表 (db_feiQ_config)
```sql
CREATE TABLE db_feiQ_config (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    config_name VARCHAR(50) DEFAULT 'default' COMMENT '配置名称',
    server_url VARCHAR(200) NOT NULL COMMENT '服务器地址',
    server_port INT DEFAULT 8080 COMMENT '服务器端口',
    receivers VARCHAR(500) NOT NULL COMMENT '接收人列表（逗号分隔）',
    enabled TINYINT DEFAULT 1 COMMENT '是否启用',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='飞秋配置表';
```

#### 2.5.4 定时任务配置表 (db_schedule_config)
```sql
CREATE TABLE db_schedule_config (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    env VARCHAR(10) NOT NULL COMMENT '环境 (DEV/SIT/UAT)',
    job_name VARCHAR(50) NOT NULL COMMENT '任务名称',
    job_description VARCHAR(200) COMMENT '任务描述',
    cron_expression VARCHAR(50) NOT NULL COMMENT 'Cron表达式',
    enabled TINYINT DEFAULT 1 COMMENT '是否启用',
    last_run_time DATETIME COMMENT '上次执行时间',
    next_run_time DATETIME COMMENT '下次执行时间',
    run_count INT DEFAULT 0 COMMENT '执行次数',
    success_count INT DEFAULT 0 COMMENT '成功次数',
    fail_count INT DEFAULT 0 COMMENT '失败次数',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_env (env)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='定时任务配置表';
```

#### 2.5.5 执行记录表 (db_execute_record)
```sql
CREATE TABLE db_execute_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    record_no VARCHAR(32) NOT NULL COMMENT '记录编号',
    env VARCHAR(10) NOT NULL COMMENT '环境',
    ds_key VARCHAR(50) NOT NULL COMMENT '数据源标识',
    db_name VARCHAR(100) NOT NULL COMMENT '数据库名',
    trigger_type VARCHAR(20) NOT NULL COMMENT '触发类型 (MANUAL/SCHEDULE)',
    trigger_by BIGINT COMMENT '触发人ID（手动触发时）',
    start_time DATETIME NOT NULL COMMENT '开始时间',
    end_time DATETIME COMMENT '结束时间',
    duration_ms BIGINT COMMENT '执行时长(毫秒)',
    status VARCHAR(20) NOT NULL COMMENT '状态 (RUNNING/SUCCESS/FAILED)',
    table_diff_count INT DEFAULT 0 COMMENT '表差异数',
    column_diff_count INT DEFAULT 0 COMMENT '字段差异数',
    index_diff_count INT DEFAULT 0 COMMENT '索引差异数',
    standard_violation_count INT DEFAULT 0 COMMENT '贯标异常数',
    result_file_path VARCHAR(500) COMMENT '结果文件路径',
    svn_revision VARCHAR(20) COMMENT 'SVN版本号',
    error_msg TEXT COMMENT '错误信息',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_env (env),
    INDEX idx_status (status),
    INDEX idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='执行记录表';
```

#### 2.5.6 执行明细表 (db_execute_detail)
```sql
CREATE TABLE db_execute_detail (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    record_id BIGINT NOT NULL COMMENT '执行记录ID',
    diff_type VARCHAR(50) NOT NULL COMMENT '差异类型 (TABLE/COLUMN/INDEX/STANDARD)',
    sub_type VARCHAR(50) NOT NULL COMMENT '子类型',
    table_name VARCHAR(100) COMMENT '表名',
    table_comment VARCHAR(200) COMMENT '表中文名',
    column_name VARCHAR(100) COMMENT '字段名',
    column_comment VARCHAR(200) COMMENT '字段中文名',
    doc_value VARCHAR(500) COMMENT '文档值',
    db_value VARCHAR(500) COMMENT '数据库值',
    diff_description VARCHAR(500) COMMENT '差异描述',
    process_sql TEXT COMMENT '处理SQL',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_record_id (record_id),
    INDEX idx_diff_type (diff_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='执行明细表';
```

#### 2.5.7 贯标字段标准表 (db_standard_field)
```sql
CREATE TABLE db_standard_field (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    column_name VARCHAR(100) NOT NULL COMMENT '字段英文名',
    column_comment VARCHAR(200) NOT NULL COMMENT '字段中文名',
    column_type VARCHAR(50) NOT NULL COMMENT '标准字段类型',
    data_code VARCHAR(50) COMMENT '数据标准码',
    description VARCHAR(500) COMMENT '说明',
    enabled TINYINT DEFAULT 1 COMMENT '是否启用',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_column_name (column_name),
    UNIQUE KEY uk_column_comment (column_comment)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='贯标字段标准表';
```

### 2.6 系统配置

#### 2.6.1 系统配置表 (sys_config)
```sql
CREATE TABLE sys_config (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    config_key VARCHAR(100) NOT NULL COMMENT '配置键',
    config_value VARCHAR(500) COMMENT '配置值',
    description VARCHAR(200) COMMENT '配置说明',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_config_key (config_key)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统配置表';
```

## 3. 初始数据

### 3.1 超级管理员
```sql
-- 密码: admin123 (BCrypt加密)
INSERT INTO sys_user (id, user_no, username, password, real_name, status) VALUES
(1, 'EMP001', 'admin', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iAt6Z5EO', '超级管理员', 1);
```

### 3.2 初始机构
```sql
INSERT INTO sys_org (id, org_code, org_name, org_type, parent_code, level, sort_order, status) VALUES
(1, '100000', '总公司', 'HEAD', '0', 1, 1, 1),
(2, '110000', '北京分公司', 'BRANCH', '100000', 2, 1, 1),
(3, '120000', '上海分公司', 'BRANCH', '100000', 2, 2, 1);
```

### 3.3 初始部门
```sql
INSERT INTO sys_dept (id, dept_code, dept_name, org_code, parent_code, sort_order, status) VALUES
(1, '100100', '技术部', '100000', '0', 1, 1),
(2, '100200', '业务部', '100000', '0', 2, 1),
(3, '110100', '北京技术部', '110000', '0', 1, 1);
```

### 3.4 初始角色
```sql
INSERT INTO sys_role (id, role_code, role_name, role_type, data_scope, status) VALUES
(1, 'super_admin', '超级管理员', 'system', 1, 1),
(2, 'admin', '系统管理员', 'system', 2, 1),
(3, 'user', '普通用户', 'system', 4, 1);
```

### 3.5 初始菜单
```sql
INSERT INTO sys_menu (id, menu_code, menu_name, parent_id, menu_type, icon, path, component, permission, sort_order, status) VALUES
-- 系统设置
(1, 'system', '系统设置', 0, 1, 'Setting', '/system', NULL, NULL, 100, 1),
(2, 'user', '用户管理', 1, 2, 'User', '/system/user', 'system/user/index', 'user:list', 1, 1),
(3, 'user:add', '新增用户', 2, 3, NULL, NULL, NULL, 'user:add', 1, 1),
(4, 'user:edit', '编辑用户', 2, 3, NULL, NULL, NULL, 'user:edit', 2, 1),
(5, 'user:delete', '删除用户', 2, 3, NULL, NULL, NULL, 'user:delete', 3, 1),
(6, 'org', '机构管理', 1, 2, 'Office', '/system/org', 'system/org/index', 'org:list', 2, 1),
(7, 'dept', '部门管理', 1, 2, 'School', '/system/dept', 'system/dept/index', 'dept:list', 3, 1),
(8, 'role', '角色管理', 1, 2, 'UserFilled', '/system/role', 'system/role/index', 'role:list', 4, 1),
(9, 'menu', '菜单管理', 1, 2, 'Menu', '/system/menu', 'system/menu/index', 'menu:list', 5, 1),
(10, 'log', '审计日志', 1, 2, 'Document', '/system/log', 'system/log/index', 'log:list', 6, 1),

-- 数据库管理
(20, 'db', '数据库管理', 0, 1, 'DataLine', '/db', NULL, NULL, 200, 1),
(21, 'dbcompare', '数据库比对', 20, 2, 'DataAnalysis', '/db/compare', 'db/compare/index', 'dbcompare:exec', 1, 1),
(22, 'dbcompare:dev', 'DEV环境比对', 21, 3, NULL, NULL, NULL, 'dbcompare:dev', 1, 1),
(23, 'dbcompare:sit', 'SIT环境比对', 21, 3, NULL, NULL, NULL, 'dbcompare:sit', 2, 1),
(24, 'dbcompare:uat', 'UAT环境比对', 21, 3, NULL, NULL, NULL, 'dbcompare:uat', 3, 1),
(25, 'dbcompare:all', '全环境比对', 21, 3, NULL, NULL, NULL, 'dbcompare:all', 4, 1),
(26, 'dbschedule', '定时任务配置', 20, 2, 'Timer', '/db/schedule', 'db/schedule/index', 'dbschedule:config', 2, 1),
(27, 'dbrecord', '执行记录', 20, 2, 'List', '/db/record', 'db/record/index', 'dbrecord:list', 3, 1);
```

### 3.6 用户角色关联
```sql
INSERT INTO sys_user_role (user_id, role_id) VALUES
(1, 1);  -- admin 拥有超级管理员角色
```

### 3.7 角色菜单关联（超级管理员拥有所有权限）
```sql
INSERT INTO sys_role_menu (role_id, menu_id)
SELECT 1, id FROM sys_menu;
```

### 3.8 用户机构关联
```sql
INSERT INTO sys_user_org (user_id, org_code, is_default) VALUES
(1, '100000', 1),
(1, '110000', 0),
(1, '120000', 0);
```

### 3.9 数据库比对定时任务
```sql
INSERT INTO db_schedule_config (env, job_name, job_description, cron_expression, enabled) VALUES
('DEV', 'devCompareJob', 'DEV环境数据库比对', '0 0 2 * * ?', 1),
('SIT', 'sitCompareJob', 'SIT环境数据库比对', '0 0 4 * * ?', 1),
('UAT', 'uatCompareJob', 'UAT环境数据库比对', '0 0 6 * * ?', 1);
```

### 3.10 系统配置
```sql
INSERT INTO sys_config (config_key, config_value, description) VALUES
('excel.menu.start.row', '14', '目录Sheet起始行'),
('excel.column.start.row', '1', '字段Sheet起始行'),
('excel.index.start.row', '1', '索引Sheet起始行'),
('svn.auto.commit', 'true', '是否自动提交SVN'),
('feiQ.enabled', 'true', '是否启用飞秋通知'),
('security.max-login-fail', '5', '最大登录失败次数'),
('security.lock-duration', '30', '账号锁定时间(分钟)');
```

## 4. ER图

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   sys_user      │     │  sys_user_role  │     │   sys_role      │
├─────────────────┤     ├─────────────────┤     ├─────────────────┤
│ PK id           │◄────┤ PK id           │────►│ PK id           │
│    user_no      │     │    user_id      │     │    role_code    │
│    username     │     │    role_id      │     │    role_name    │
│    password     │     └─────────────────┘     │    data_scope   │
│    org_code     │                             │    ...          │
│    dept_code    │                             └────────┬────────┘
│    is_locked    │                                      │
└─────────────────┘                                      │
        ▲                                                │
        │                                                │
        │                       ┌─────────────────────────┘
        │                       │
        │                       ▼
        │             ┌─────────────────┐
┌───────┴───────┐     │  sys_role_menu  │
│ sys_user_org  │     ├─────────────────┤
├───────────────┤     │ PK id           │
│ PK id         │     │    role_id      │
│    user_id    │     │    menu_id      │
│    org_code   │     └────────┬────────┘
│    is_default │              │
└───────────────┘              ▼
                               ┌─────────────────┐
                               │   sys_menu      │
                               ├─────────────────┤
                               │ PK id           │
                               │    menu_code    │
                               │    menu_name    │
                               │    parent_id    │
                               │    permission   │
                               └─────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      数据库比对模块                              │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │db_datasource_cfg│  │  db_svn_config  │  │ db_feiQ_config  │  │
│  ├─────────────────┤  ├─────────────────┤  ├─────────────────┤  │
│  │ PK id           │  │ PK id           │  │ PK id           │  │
│  │    env          │  │    svn_url      │  │    server_url   │  │
│  │    ds_key       │  │    username     │  │    receivers    │  │
│  │    db_name      │  └─────────────────┘  └─────────────────┘  │
│  └─────────────────┘                                            │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │db_schedule_cfg  │  │db_execute_record│  │db_execute_detail│  │
│  ├─────────────────┤  ├─────────────────┤  ├─────────────────┤  │
│  │ PK id           │  │ PK id           │  │ PK id           │  │
│  │    env          │  │    env          │◄─┤    record_id    │  │
│  │    cron_expr    │  │    ds_key       │  │    diff_type    │  │
│  └─────────────────┘  │    status       │  │    table_name   │  │
│                       └─────────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

**文档版本**: v3.0  
**编写日期**: 2025-01-15  
**更新说明**: 整合现有项目用户认证、机构部门、权限模型设计
